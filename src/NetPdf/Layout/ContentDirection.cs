namespace NetPdf.Layout;

/// <summary>Horizontal flow direction for content layout and text.</summary>
public enum ContentDirection
{
    /// <summary>Left-to-right (the default).</summary>
    LeftToRight,
    /// <summary>Right-to-left: text defaults to right alignment and rows/inlined items mirror.</summary>
    RightToLeft,
}
