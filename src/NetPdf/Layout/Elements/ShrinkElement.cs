namespace NetPdf.Layout.Elements;

/// <summary>
/// Occupies only the child's natural size. Wrap it around content placed inside expanding
/// parents (e.g. an extended column item) to keep the content at its measured size.
/// </summary>
public sealed class ShrinkElement : ContainerElement
{
}
